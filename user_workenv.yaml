---
    comments: "override some UPS products with newer versions; 
      setting up some additioanl UPS products;
      add additional source packages"

    base_release: develop

    product_paths: ~

    externals: ~

    packages: ~

    repos: ~
        
    workdirs:
        - build: ./build
        - log: ./log
        - install: ./install
        - source: ./sourcecode
