if (${?OPERA}) then
  setenv upstmp "FIDELIO"
  setenv OPERA "`$UPS_DIR/bin/dropit -e -p "'"$OPERA"'" -i' ' -d' ' "'"$upstmp"'"`"
  if ("${OPERA}" == "") unsetenv OPERA
  unsetenv upstmp
endif
#
if (${?OPERA}) then
  setenv upstmp "MADAM_BUTTERFLY CARMEN"
  setenv OPERA "`$UPS_DIR/bin/dropit -e -p "'"$OPERA"'" -i' ' -d' ' "'"$upstmp"'"`"
  if ("${OPERA}" == "") unsetenv OPERA
  unsetenv upstmp
endif
#
unsetenv OPERA
#
if (${?SYMPHONY}) then
  setenv upstmp "EROICA"
  setenv SYMPHONY "`$UPS_DIR/bin/dropit -e -p "'"$SYMPHONY"'" -i';' -d';' "'"$upstmp"'"`"
  if ("${SYMPHONY}" == "") unsetenv SYMPHONY
  unsetenv upstmp
endif
#
unsetenv FLY_DIR
#
unsetenv SETUP_FLY
#
